const inputNumber = document.querySelector("#cafeNumber");
const inputWeight = document.querySelector("#enterWeight");
const button = document.querySelector("button");

button.addEventListener("click", e => {
  e.preventDefault(); // Zapobiega odswierzaniu sie strony
});
